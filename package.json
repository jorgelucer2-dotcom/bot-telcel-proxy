{
  "name": "bot-telcel-proxy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.48.0",
    "telegraf": "^4.16.3"
  },
  "engines": {
    "node": "20.x"
  }
}


