{
  "name": "bot-telcel-proxy",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "telegraf": "^4.16.3",
    "playwright": "^1.48.0",
    "dotenv": "^16.4.5",
    "http": "^0.0.1-security"
  },
  "scripts": {
    "start": "node index.js",
    "build": "PLAYWRIGHT_BROWSERS_PATH=/tmp/playwright-browsers npm install && npx playwright install chromium --path /tmp/playwright-browsers"
  }
}
